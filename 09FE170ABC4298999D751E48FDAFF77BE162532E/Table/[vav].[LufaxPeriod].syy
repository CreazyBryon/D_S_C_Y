SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO
IF NOT EXISTS (SELECT * FROM sys.objects WHERE object_id = OBJECT_ID(N'[vav].[LufaxPeriod]') AND type in (N'U'))
BEGIN
CREATE TABLE [vav].[LufaxPeriod](
	[Id] [int] NOT NULL,
	[Unit] [nvarchar](10) NOT NULL
) ON [PRIMARY]
END
GO
