SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO
IF NOT EXISTS (SELECT * FROM sys.objects WHERE object_id = OBJECT_ID(N'[vav].[CalendarCHN]') AND type in (N'U'))
BEGIN
CREATE TABLE [vav].[CalendarCHN](
	[date] [datetime] NULL,
	[isholiday] [int] NULL,
	[lastupdate] [datetime] NULL
) ON [PRIMARY]
END
GO
