SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO
IF NOT EXISTS (SELECT * FROM sys.objects WHERE object_id = OBJECT_ID(N'[MySteel].[DeleteCode]') AND type in (N'U'))
BEGIN
CREATE TABLE [MySteel].[DeleteCode](
	[code] [nvarchar](255) NULL
) ON [PRIMARY]
END
GO
